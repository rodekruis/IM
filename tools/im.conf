description "IM Application server"
author      "Netherlands Red Cross"

# used to be: start on startup
# until we found some mounts weren't ready yet while booting:
#start on started mountall 
stop on shutdown

# Automatically Respawn:
respawn
respawn limit 99 5

script
    # Not sure why $HOME is needed, but we found that it is:
    export HOME="/root"
    chdir /home/fAWsxiPd48evqcc/IM/
    echo $$ > /var/run/app.pid
    exec sudo -u fAWsxiPd48evqcc NODE_ENV=production /usr/bin/nodejs server.js >> /var/log/im.log 2>&1
end script

post-start script
   # Optionally put a script here that will notifiy you node has (re)started
    /root/bin/hoptoad.sh "IM has started!"
end script